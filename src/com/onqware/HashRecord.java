package com.onqware;

// A linked list node where
// - value: BridgeEntry
// - next: Pointer to another linked list node
public class HashRecord {
    public BridgeEntry value = null;
    public HashRecord left = null;
    public HashRecord right = null;
}
